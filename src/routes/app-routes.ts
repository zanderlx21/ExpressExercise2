import express from "express";
const routes = express.Router();

// routes.get("/", function (req, res) {
//   res.render("home");
// });

// routes.get("/shops-list", function (req, res) {
//   res.render("shops-list", { shops });
// });

export default routes;
